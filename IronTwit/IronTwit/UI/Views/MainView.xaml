<Window x:Class="Unite.UI.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:Unite.UI.Controls"
    Title="Window1" Width="437" Height="504">
	<Window.Resources>
		<Color x:Key="BackgroundDark">#FFD3FD9E</Color>
		<LinearGradientBrush x:Key="LightGradientBackGround" EndPoint="0.5,1" StartPoint="0.5,0">
			<GradientStop Color="#FFFFFFFF" Offset="0"/>
			<GradientStop Color="#FFD3FD9E" Offset="1"/>
		</LinearGradientBrush>
	</Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Text="Inbox" />  
        <ListBox Grid.Row="1" Grid.IsSharedSizeScope="True" MinHeight="100" 
                 x:Name="Inbox" 
                 ItemsSource="{Binding Messages}" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Visible" Background="{DynamicResource LightGradientBackGround}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Controls:MessageView />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="2" Background="{DynamicResource LightGradientBackGround}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition MinHeight="100" />
            </Grid.RowDefinitions>
            <TextBox x:Name="Recipient" Text="{Binding Recipient}" Grid.Row="0" Grid.Column="0" />
            <Button x:Name="SendMessage" Grid.Row="0" Grid.Column="1" 
                    Command="{Binding SendMessage}" 
                    Content="Send" />
            <Button x:Name="ReceiveMessage" Grid.Row="0" Grid.Column="2" 
                    Command="{Binding ReceiveMessage}" 
                    Content="Refresh" />
            <TextBox x:Name="MessageToSend" Text="{Binding MessageToSend}" Grid.Row="1" Grid.ColumnSpan="3" Background="{DynamicResource LightGradientBackGround}" />
        </Grid>
    </Grid>
</Window>
