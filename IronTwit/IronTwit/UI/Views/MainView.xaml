<Window x:Class="Unite.UI.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:Unite.UI.Controls"
    Title="Window1" Width="437" Height="504">
	<Window.Resources>
		<Color x:Key="BackgroundDark">#FFD3FD9E</Color>
		<LinearGradientBrush x:Key="LightGradientBackGround" EndPoint="0.5,1" StartPoint="0.5,0">
			<GradientStop Color="#FFFFFFFF" Offset="0"/>
			<GradientStop Color="#FFD3FD9E" Offset="1"/>
		</LinearGradientBrush>
	</Window.Resources>
	<Window.Background>
		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
			<GradientStop Color="#FF5B954E" Offset="0"/>
			<GradientStop Color="#FFE5EF62" Offset="0.085"/>
		</LinearGradientBrush>
	</Window.Background>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Inbox" />
            <ComboBox Grid.Column="1" x:Name="SenderFilter" />
            <TextBox Grid.Column="2" x:Name="SearchBox" Text="Filter on..." />
        </Grid>
        <ListBox Grid.Row="1" Grid.IsSharedSizeScope="True" MinHeight="100" 
                 x:Name="Inbox" 
                 ItemsSource="{Binding Messages}" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Visible" AllowDrop="False" Opacity="1">
        	<ListBox.Background>
        		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
        			<GradientStop Color="#FFFFFFFF" Offset="0.022"/>
        			<GradientStop Color="#FFBBC4C4" Offset="0"/>
        		</LinearGradientBrush>
        	</ListBox.Background>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Controls:MessageView />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="2" Background="{DynamicResource LightGradientBackGround}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition MinHeight="100" />
            </Grid.RowDefinitions>
            <TextBox x:Name="Recipient" Text="{Binding Recipient}" Grid.Row="0" Grid.Column="0" >
            	<TextBox.Background>
            		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            			<GradientStop Color="#FFC8C8C8" Offset="0"/>
            			<GradientStop Color="#FFFFFFFF" Offset="0.388"/>
            			<GradientStop Color="#FFDFDFDF" Offset="0.134"/>
            		</LinearGradientBrush>
            	</TextBox.Background>
            </TextBox>
            <Button x:Name="SendMessage" Grid.Row="0" Grid.Column="1" 
                    Command="{Binding SendMessage}" 
                    Content="Send" />
            <Button x:Name="ReceiveMessage" Grid.Row="0" Grid.Column="2" 
                    Command="{Binding ReceiveMessage}" 
                    Content="Refresh" />
            <TextBox x:Name="MessageToSend" Text="{Binding MessageToSend}" Grid.Row="1" Grid.ColumnSpan="3" >
            	<TextBox.Background>
            		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            			<GradientStop Color="#FFFFFFFF" Offset="0.076"/>
            			<GradientStop Color="#FFCFCFCF" Offset="0"/>
            		</LinearGradientBrush>
            	</TextBox.Background>
            </TextBox>
        </Grid>
    </Grid>
</Window>
